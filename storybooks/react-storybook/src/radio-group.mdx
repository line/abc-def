import { Canvas, Controls, Meta, Story } from "@storybook/addon-docs/blocks";

import { Toaster } from "@line/abc-def-react";

import ThemeSelect from "./utils/themeSelect.tsx";
import TokenTable from "./utils/tokenTable.tsx";

import * as RadioStories from "./radio-group.stories.tsx";

<Meta title="RadioGroup" of={RadioStories} />

# Radio Group

<ThemeSelect />

## Playground

<Canvas of={RadioStories.Default} />
<Controls />

## Horizontal

<Canvas of={RadioStories.Horizontal} />

## Vertical

<Canvas of={RadioStories.Vertical} />

## Tokens

<TokenTable
  items={[
    {
      name: "--radio-group-fg-label",
      value: "var(--fg-neutral-primary)",
    },
    {
      name: "--radio-group-border",
      value: "var(--border-neutral-tertiary)",
    },
    {
      name: "--radio-group-bg",
      value: "var(--bg-neutral-primary)",
    },
    {
      name: "--radio-group-bg-selected",
      value: "var(--tint, var(--bg-neutral-inverse))",
    },
  ]}
/>

<Toaster className="sb-unstyled" position="bottom-right" />
