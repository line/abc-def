# @abc-def/react

React components built on the shared abc-def styling contract.

## Styling

Import `@abc-def/styles/css` in your application stylesheet. Tailwind v4 source registration belongs in app CSS, not in a shared JavaScript config layer.
